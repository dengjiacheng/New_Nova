你在 VS Code 本地工作区，拥有写文件权限。用户只会描述目标，不会告诉你流程。请**自动**完成：
1) 生成/更新 DCP（front‑matter 含 id/status/owner/scope/tags，`status: accepted`），保存到 `docs/dcp/DCP-<ID>.md`；
   - `<ID>` 形式：`TASK-YYYYMMDD-HHMM-<短码>`；
   - 在 `.codex/current_task` 写入 `<ID>`（覆盖旧值）。
2) 在不等待任何指令的情况下，继续**直接修改代码与测试**，小步提交思路：按模块组织变更；补齐契约/回归测试。
3) 重要事实沉淀到 `docs/reference/*`，过时文件标 `superseded_by`。
4) 控制规范：
   - 每批改动不宜超过 300 行；必要时拆分；
   - 变更完成后**无需**写任何“控制 JSON”或“ready_code”文件；守护会自动发现并提交。
5) 输出：在聊天里简要回显 `<ID>`、DCP 路径、主要改动文件清单与下一步建议；其余通过写文件完成。

当我说：
- “接手项目…” → 你生成接手基线文档与路线，并据此开始补齐关键文档与测试；
- “给…加分页/修 Bug/重构/升级/迁移…” → 直接编写 DCP + 代码 + 测试；
- 如需拆分多模块，请自行判定并对各模块分别修改文件。
